@model ActiveMenu.ViewModel.detailVM
@{
    ViewBag.Title = "Detail";
    ViewBag.ItemTitle = Model.name;
}



@*<div style="position:relative" class="headerTop">
    <div>
        <a href="/Home/basket" class="cartHolder">
            <p class="simpleCart_quantityy" style="color:white; margin-left:10px;font-size:20px;"></p>
            <i class="fal fa-shopping-cart" style=" font-size:20px; color:white"></i>
        </a>

    </div>
    <span style="flex:1"></span>
    <div style=" text-align:right; display:flex; align-items:center">
        <p style="margin-right:10px ; color:white;font-size:20px; margin-bottom:0">@</p>
        <i class="fal fa-long-arrow-right" style="font-size:20px; color:white; "></i>
    </div>


</div>*@


<div style="position:relative" class="itemparent">
    <div style="text-align:center;display: flex;justify-content: center;margin-bottom: 25px;">
        <div style="height:2px; background-color:orange; width:30%"></div>
    </div>

    @foreach (var item in Model.itemList)
    {
        @Html.Partial("~/Views/Shared/ItemSection.cshtml", item);

    }
</div>
<style>

    .itemparent {
       
    }

    .itemHolder {
        margin: 10px;
        background: rgba(255,255,255,0.05);
        backdrop-filter: blur(10px);
        border-top: 1px solid rgba(255,255,255,0.2);
        border-left: 1px solid rgba(255,255,255,0.2);
        box-shadow: 5px 5px 30px rgba(0,0,0,0.2);
        width:80%;
    }

    .countButton {
        background: rgba(255,255,255,0.05);
        backdrop-filter: blur(10px);
        border-top: 1px solid rgba(255,255,255,0.2);
        border-left: 1px solid rgba(255,255,255,0.2);
        box-shadow: 5px 5px 30px rgba(0,0,0,0.2);
    }
</style>

@section scripts{

}